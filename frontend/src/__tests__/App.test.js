it("<Search/> should work", () => {
  expect(1).toBe(1);
});

it("<SubmitClaim/> should work", () => {
  expect(1).toBe(1);
});

it("<CardDetail/> should work", () => {
  expect(1).toBe(1);
});

it("<Browse/> should work", () => {
  expect(1).toBe(1);
});
